export type LgoinType = {
    email: string,
    password: string
}

export type RegistrationType = {
    name: string,
    email: string,
    password: string
}


export type UserLoginType = {
    email: String
    name: String
    user_id: Number
}

export type LogOutType = {
    user: boolean
    message: string
}
